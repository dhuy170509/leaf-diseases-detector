{
    "$schema": "https://railway.com/schema.json",
    "build": {
        "builder": "nixpacks"
    },
    "deploy": {
        "numReplicas": 1,
        "startCommand": "uvicorn api_server:app --host 0.0.0.0 --port $PORT"
    }
}